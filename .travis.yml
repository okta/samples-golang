language: go

go:
  - 1.16.x

install:
  - npm install

script:
  - npm test
  - cd identity-engine/embedded-auth-with-sdk/ && go test -v
  
env:
  global:

addons:
  sauce_connect: true
  apt:
    packages:
    - google-chrome-stable
    - nodejs

